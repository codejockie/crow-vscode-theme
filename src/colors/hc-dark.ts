import type { Colors } from ".";
import { base, tokenColor, uiColor } from "./dark";

const colors: Colors = {
  baseColor: base,
  tokenColor,
  uiColor,
  theme: "hc-dark",
};
export default colors;
